<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Jellyfin.Plugin.Meilisearch</RootNamespace>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <Nullable>enable</Nullable>
    <AnalysisMode>AllEnabledByDefault</AnalysisMode>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Jellyfin.Controller" Version="12.0.0-*" />
    <PackageReference Include="Jellyfin.Model" Version="12.0.0-*" />
    <PackageReference Include="Jellyfin.Common" Version="12.0.0-*" />
  </ItemGroup>

  <!-- Meilisearch SDK -->
  <ItemGroup>
    <PackageReference Include="Meilisearch" Version="0.18.0" />
  </ItemGroup>

  <!--
    Local embedding model for semantic search. ONNX Runtime brings a native library per platform;
    CopyLocalLockFileAssemblies lays the runtimes/ tree out next to the plugin assembly so the
    P/Invoke resolves inside Jellyfin's plugin load context.
  -->
  <ItemGroup>
    <PackageReference Include="Microsoft.ML.OnnxRuntime" Version="1.29.0" />
    <PackageReference Include="Microsoft.ML.Tokenizers" Version="2.0.0" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SerilogAnalyzer" Version="0.15.0" PrivateAssets="All" />
    <PackageReference Include="StyleCop.Analyzers" Version="1.2.0-beta.556" PrivateAssets="All" />
    <PackageReference Include="SmartAnalyzers.MultithreadingAnalyzer" Version="1.1.31" PrivateAssets="All" />
  </ItemGroup>

  <ItemGroup>
    <None Remove="Configuration\configPage.html" />
    <EmbeddedResource Include="Configuration/configPage.html" />
  </ItemGroup>

  <!--
    Restage ONNX Runtime's native libraries out of runtimes/ and into native/.

    Jellyfin's plugin loader discovers assemblies with
    Directory.GetFiles(pluginDir, "*.dll", SearchOption.AllDirectories) and calls
    LoadFromAssemblyPath on every hit. A native Windows onnxruntime.dll under runtimes/ is not a
    managed assembly, so that call throws and the catch-all marks the entire plugin Malfunctioned -
    on every platform, not just Windows. Giving the Windows natives a non-.dll extension keeps them
    out of that scan; OnnxRuntimeNativeLoader loads them by full path, which does not care about the
    extension. The .so and .dylib natives are never scanned, so they keep their real names.

    This also drops the mobile natives and Windows link libraries, which a server cannot use and
    which account for over 100 MB.

    It has to happen for `dotnet publish` as well as `dotnet build`, not just one of them: JPRM
    packages the publish output, and publish resolves the runtimes/ tree from the NuGet assets
    independently of whatever the build left in bin/. Staging only after Build produces a package
    with no native/ directory at all, and the plugin then fails to load ONNX Runtime at runtime.
  -->
  <ItemGroup>
    <!-- Source path relative to the output root, paired with where it is staged to. -->
    <OnnxRuntimeNative Include="runtimes/linux-x64/native/libonnxruntime.so">
      <StagedPath>native/linux-x64/libonnxruntime.so</StagedPath>
    </OnnxRuntimeNative>
    <OnnxRuntimeNative Include="runtimes/linux-arm64/native/libonnxruntime.so">
      <StagedPath>native/linux-arm64/libonnxruntime.so</StagedPath>
    </OnnxRuntimeNative>
    <OnnxRuntimeNative Include="runtimes/osx-arm64/native/libonnxruntime.dylib">
      <StagedPath>native/osx-arm64/libonnxruntime.dylib</StagedPath>
    </OnnxRuntimeNative>
    <OnnxRuntimeNative Include="runtimes/osx-x64/native/libonnxruntime.dylib">
      <StagedPath>native/osx-x64/libonnxruntime.dylib</StagedPath>
    </OnnxRuntimeNative>
    <OnnxRuntimeNative Include="runtimes/win-x64/native/onnxruntime.dll">
      <StagedPath>native/win-x64/onnxruntime.nativelib</StagedPath>
    </OnnxRuntimeNative>
    <OnnxRuntimeNative Include="runtimes/win-arm64/native/onnxruntime.dll">
      <StagedPath>native/win-arm64/onnxruntime.nativelib</StagedPath>
    </OnnxRuntimeNative>
  </ItemGroup>

  <Target Name="StageOnnxRuntimeNativesInBuildOutput" AfterTargets="Build">
    <MSBuild Projects="$(MSBuildProjectFullPath)"
             Targets="StageOnnxRuntimeNatives"
             Properties="OnnxNativeStageRoot=$(OutDir);TargetFramework=$(TargetFramework)" />
  </Target>

  <Target Name="StageOnnxRuntimeNativesInPublishOutput" AfterTargets="Publish">
    <MSBuild Projects="$(MSBuildProjectFullPath)"
             Targets="StageOnnxRuntimeNatives"
             Properties="OnnxNativeStageRoot=$(PublishDir);TargetFramework=$(TargetFramework)" />
  </Target>

  <Target Name="StageOnnxRuntimeNatives">
    <ItemGroup>
      <OnnxNativeToStage Include="@(OnnxRuntimeNative)" Condition="Exists('$(OnnxNativeStageRoot)%(Identity)')" />
    </ItemGroup>

    <Copy SourceFiles="@(OnnxNativeToStage->'$(OnnxNativeStageRoot)%(Identity)')"
          DestinationFiles="@(OnnxNativeToStage->'$(OnnxNativeStageRoot)%(StagedPath)')"
          SkipUnchangedFiles="true" />
    <RemoveDir Directories="$(OnnxNativeStageRoot)runtimes" ContinueOnError="true" />
  </Target>
</Project>
